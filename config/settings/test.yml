stacks:
  root: <%= File.join(Rails.root, "spec", "fixtures", "stacks") %>

s3:
  access_key_id: rustfs-user
  secret_access_key: rustfs-password
  bucket: stacks-test
  endpoint: http://localhost:9000

content_search:
  url: "http://example.com/content_search/%{druid}/search"

resource_cache:
  enabled: false
