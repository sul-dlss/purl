name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: ["*"]

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ruby: ["4.0"]
    steps:
      - uses: actions/checkout@v6
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - name: Audit dependencies
        run: bin/bundler-audit
      - name: Start RustFS
        run: docker compose up -d rustfs_server
      - name: Wait for RustFS to be ready
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:9000/health; do sleep 2; done'
      - name: Run tests
        run: bin/rake test:prepare spec
      - name: Stop RustFS
        if: always()
        run: docker compose down
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4
          bundler-cache: true
      - name: Lint Ruby files
        run: bin/rubocop --parallel
