<div class="row">
  <div class="col-12">
    <h1 class="py-2"><%= display_title %></h1>
  </div>
</div>

<% unless withdrawn? %>
  <%= render EmbedComponent.new(version:) %>


  <div class="record-metadata row gx-5">
    <%= render Show::BodyComponent.new(version:) %>
    <%= render Show::SidebarComponent.new(version:, purl:) %>

  </div>
<% end %>
