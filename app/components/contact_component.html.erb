<%= render SectionComponent.new(label: 'Contact information') do %>
  <dl>
    <% contact_email_display_data.each do |contact| %>
      <dt class="visually-hidden"><%= contact.label %></dt>
      <dd><%= auto_link contact.values.first %></dd>
    <% end %>
  </dl>
<% end %>
