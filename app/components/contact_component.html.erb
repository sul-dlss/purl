<%= render SectionComponent.new(label: 'Contact information') do %>
  <dl>
    <% contacts.each do |contact| %>
      <dt class="visually-hidden"><%= contact['displayLabel'] %></dt>
      <dd><%= auto_link contact['value'] %></dd>
    <% end %>
  </dl>
<% end %>
