<script type="text/javascript">
  var peStacksURL = "<%= Settings.stacks.url %>";
  var purlServerURL = "<%= request.scheme %>" + "://" + "<%= request.host_with_port %>";
  var peServerURL = purlServerURL;  
  var pePid = "<%= @purl.pid %>";
  var peImgInfo = [ <%= get_image_json_array.html_safe %> ];  
</script>
